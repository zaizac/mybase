import { VisibleStepperPipe } from './visible-stepper.pipe';

describe('VisibleStepperPipe', () => {
  it('create an instance', () => {
    const pipe = new VisibleStepperPipe();
    expect(pipe).toBeTruthy();
  });
});

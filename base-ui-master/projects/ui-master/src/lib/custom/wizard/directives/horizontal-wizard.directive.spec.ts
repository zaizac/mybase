import { HorizontalWizardDirective } from './horizontal-wizard.directive';

describe('HorizontalWizardDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalWizardDirective();
    expect(directive).toBeTruthy();
  });
});

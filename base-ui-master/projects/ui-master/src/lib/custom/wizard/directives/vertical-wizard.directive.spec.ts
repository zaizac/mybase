import { VerticalWizardDirective } from './vertical-wizard.directive';

describe('VerticalWizardDirective', () => {
  it('should create an instance', () => {
    const directive = new VerticalWizardDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});

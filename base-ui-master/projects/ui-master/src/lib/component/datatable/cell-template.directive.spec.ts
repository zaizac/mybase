import { CellTemplateDirective } from './cell-template.directive';

describe('CellTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new CellTemplateDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
